# The specific ticket name you are tracking (e.g., 'ticket-A')
booth_ticket_name: "ticket-A"

# The path to the configuration file
booth_conf_path: "/etc/booth/booth.conf"

# The Inventory group name containing all cluster nodes AND booth arbitrators
# This is used to compare config files across all relevant servers
booth_all_nodes_group: "all"